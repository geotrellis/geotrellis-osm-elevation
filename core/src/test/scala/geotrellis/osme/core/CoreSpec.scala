package geotrellis.osme.core

import org.scalatest._

class CoreSpec extends FunSpec with Matchers {
  describe("Core spec") {
    it("should test some mock code") {
      Core() should be (45)
    }
  }
}
